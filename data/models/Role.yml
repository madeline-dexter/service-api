Role:
  tableName: role
  fields:
    id:
      type: INTEGER
      primaryKey: true
      autoIncrement: true
    name:
      type: STRING
      allowNull: false
      unique: true
    description:
      type: STRING
      allowNull: false
  options:
    timestamps: true
    underscored: true
  # associations:
  #   - type: belongsToMany
  #     through: UserRole
  #     foreignKey: userId
  #     as: roles
